{
  "name": "tsnodeapi",
  "version": "1.0.0",
  "description": "API node com typescript",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch src/http/server.ts --onSuccess \"node dist/index.js\"",
    "buid": "tsup src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.13",
    "tsx": "^4.11.0",
    "typescript": "^5.4.5"
  }
}

// "buid": "tsup src/http/server.ts --dts --minify --format cjs --out-dir dist",